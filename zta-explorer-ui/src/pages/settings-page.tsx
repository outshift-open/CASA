import {Settings} from 'lucide-react';
import {PlaceholderPage} from '@/components/placeholder-page';

export function SettingsPage() {
    return (
        <PlaceholderPage
            title="Settings"
            description="Configure your authentication server settings and preferences. This page is under construction."
            icon={Settings}
        />
    );
}
