apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.agentSafe.serviceName }}
  namespace: '{{ .Release.Namespace }}'
  labels:
    app.kubernetes.io/name: {{ .Values.agentSafe.serviceName }}
    tier: backend
spec:
  ports:
  - name: http
    protocol: TCP
    port: {{ .Values.agentSafe.servicePort }}
    targetPort: {{ .Values.agentSafe.servicePort }}
  type: ClusterIP
  selector:
    app: {{ .Values.agentSafe.serviceName }}
