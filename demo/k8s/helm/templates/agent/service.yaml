apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.agent.serviceName }}
  namespace: '{{ .Release.Namespace }}'
  labels:
    app.kubernetes.io/name: {{ .Values.agent.serviceName }}
    tier: backend
spec:
  ports:
  - name: http
    protocol: TCP
    port: {{ .Values.agent.servicePort }}
    targetPort: {{ .Values.agent.servicePort }}
  type: ClusterIP
  selector:
    app: {{ .Values.agent.serviceName }}
