apiVersion: v1
kind: Service
metadata:
  name: chat-ui
  namespace: default
  labels:
    app: chat-ui
    component: demo
spec:
  type: ClusterIP
  selector:
    app: chat-ui
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
