# Copyright 2025 Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

[project]
name = "identity-web-and-file-system-mcp-sample-server"
version = "0.0.0"
description = "Sample Web and File System MCP Server"
requires-python = ">=3.12"
dependencies = [
    "uvicorn",
    "fastapi[standard]",
    "httpx",
    "mcp",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.uv.sources]
a2a-samples = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
